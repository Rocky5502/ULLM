% Live main-study execution-validation record, 2026-09-03.
% This is an execution/provenance record. Do not promote any number below into RQ Results
% until the corresponding frozen run passes all manifest, completion-budget, model-control,
% checksum, and analysis evidence gates.

\paragraph{Live main-study execution checkpoint (2026-09-03).}
The frozen neutral deterministic stage completed all 2,000 planned calls (400 items across five gateway routes). Under pre-main Adjudication A1, all schema-valid outputs are preserved exactly as returned even when the explicit discrete label disagrees with the maximum-probability class. Stage 5 therefore passed after preserving 10/400 Claude and 8/400 Qwen decision--distribution inconsistencies as warnings rather than selectively retrying, repairing, relabeling, or deleting them. GPT, DeepSeek, and Gemini had zero such inconsistencies in that stage. The completion-budget audit checked all 2,000 responses and found zero \texttt{finish\_reason=length} rows; the model-control audit reported zero override mismatches and zero reasoning violations. The raw Stage-5 directory was sealed with a SHA-256 evidence manifest before interpretation.

The repeated-sampling stage then collected all 10,000 planned K=5 rows (2,000 per route). The first manifest-aware audit stopped the pipeline before any robustness or verifier stage because Claude had 8/2,000 parse failures (0.4\%). The other four routes had no parse failures. Adjudication A1 decision--distribution inconsistencies were preserved as non-fatal warnings in the sampling stage: Claude 46/2,000, DeepSeek 9/2,000, Qwen 60/2,000, and zero for GPT and Gemini. Gemini had 44 successful responses that required more than one HTTP attempt. Because parse failures remain a fatal scientific-record condition, Stage 6 is not yet evidence-ready and no downstream RQ analysis is authorized.

The predeclared recovery path is restricted to failed rows only: preserve a pre-retry checksum, inspect the eight Claude failure shapes plus completion-budget/model-control metadata, and invoke the frozen resume mechanism with \texttt{--retry-failures}. That mechanism removes only rows carrying a request error or missing parsed prediction and refills exactly those missing (example, repeat) keys under the unchanged manifest. Valid rows, including A1 inconsistency warnings, remain untouched. No model, prompt, temperature, sampling K, dataset, threshold, or statistical setting is changed by this recovery.
